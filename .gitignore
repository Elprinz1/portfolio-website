# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Design exploration kept locally, never published.
# Flow A (the dark direction) and the three-way hero mockups still run at
# /flow-a.html and /mockups.html on the dev server, but are not part of the
# site, the build, or the repo.
flow-a.html
flow-c.html
mockups.html
src/flow-a.*
src/flow-c.*
src/mockups.*
